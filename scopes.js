function add(a, b) {
  const factor = 0.5;
  const result = (a + b) * factor;
  return result;
}
// factor is not defined , it happens bcoz it is block scoped
// console.log(factor);

// TODO:Ask ChatGPT----> block scope,Global scope,Scope chain, TDZ (Temporal Death Zone), local scope,Hoisting
